[package]
name = "pt-core"
description = "Shared types and the Work Unit cost model for Provisioned Throughput"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
uuid.workspace = true
schemars = { workspace = true, optional = true }

[features]
schema = ["dep:schemars"]
