# Generated by `cargo run -p pt-crds --bin crdgen`. Do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: poolallocations.pt.example.com
spec:
  group: pt.example.com
  names:
    kind: PoolAllocation
    plural: poolallocations
    shortNames:
    - ptalloc
    singular: poolallocation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.pool
      name: Pool
      type: string
    - jsonPath: .spec.tenant
      name: Tenant
      type: string
    - jsonPath: .spec.tier
      name: Tier
      type: string
    - jsonPath: .spec.wuPerSec
      name: WU/s
      type: number
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for PoolAllocationSpec via `CustomResource`
        properties:
          spec:
            properties:
              burstFactor:
                default: 1.0
                description: Declared peak-to-mean ratio from the reservation's shape. Sizes the burst allowance.
                format: double
                type: number
              dedicatedWorkers:
                description: Workers reserved for this tenant on dedicated pools.
                items:
                  type: string
                type: array
              kvShare:
                default: 0.0
                description: Share of the pool's KV blocks this tenant may hold before preemption (docs/05 §4).
                format: double
                type: number
              pool:
                description: Name of a `ModelPool` in the same namespace.
                type: string
              reservation:
                type: string
              tenant:
                type: string
              tier:
                enum:
                - interactive
                - agentic
                - standard
                type: string
              wuPerSec:
                description: This reservation's WU/s entitlement on this pool.
                format: double
                type: number
            required:
            - pool
            - reservation
            - tenant
            - tier
            - wuPerSec
            type: object
        required:
        - spec
        title: PoolAllocation
        type: object
    served: true
    storage: true
