# Generated by `cargo run -p pt-crds --bin crdgen`. Do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: capacityreservations.pt.example.com
spec:
  group: pt.example.com
  names:
    kind: CapacityReservation
    plural: capacityreservations
    shortNames:
    - ptres
    singular: capacityreservation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenant
      name: Tenant
      type: string
    - jsonPath: .spec.model
      name: Model
      type: string
    - jsonPath: .spec.cus
      name: CUs
      type: integer
    - jsonPath: .spec.tier
      name: Tier
      type: string
    - jsonPath: .spec.termEnd
      name: Term ends
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for CapacityReservationSpec via `CustomResource`
        properties:
          spec:
            properties:
              boundaryPolicy:
                default:
                  burst: null
                  queue: null
                  spillover: false
                properties:
                  burst:
                    nullable: true
                    properties:
                      continuation_reserve:
                        default: 0.25
                        description: |-
                          Fraction of the credit cap reserved for `continuation` calls, so a new agent session
                          can't use up the credit a half-finished one needs.
                        format: double
                        type: number
                      max_credit_seconds:
                        default: 60.0
                        description: Unused entitlement banked as burst credit, in seconds of entitlement.
                        format: double
                        type: number
                      max_rate_multiple:
                        default: 2.0
                        description: Instantaneous ceiling as a multiple of the entitlement rate.
                        format: double
                        type: number
                    type: object
                  queue:
                    nullable: true
                    properties:
                      deadline_ms:
                        default: 2000
                        description: Longest a request may wait at the gateway, measured from receipt.
                        format: uint64
                        minimum: 0.0
                        type: integer
                      max_depth_wu_seconds:
                        default: 5.0
                        description: Queue depth limit, in seconds of entitlement.
                        format: double
                        type: number
                    type: object
                  spillover:
                    default: false
                    description: Send over-entitlement traffic to the PAYG pool, billed at PAYG list price.
                    type: boolean
                type: object
              cus:
                description: Minimum 1.
                format: uint32
                minimum: 1.0
                type: integer
              isolation:
                default: shared
                enum:
                - shared
                - dedicated
                - strict_dedicated
                type: string
              model:
                type: string
              pinnedGpuClass:
                description: GPU class for hardware-pinned reservations.
                nullable: true
                type: string
              shape:
                properties:
                  burst_factor:
                    default: 1.0
                    format: double
                    type: number
                  cache_hit_ratio:
                    default: 0.0
                    format: double
                    type: number
                  context_ceiling:
                    format: uint64
                    minimum: 0.0
                    type: integer
                  input_max:
                    format: uint64
                    minimum: 0.0
                    type: integer
                  input_p95:
                    format: uint64
                    minimum: 0.0
                    type: integer
                  output_p95:
                    format: uint64
                    minimum: 0.0
                    type: integer
                required:
                - context_ceiling
                - input_max
                - input_p95
                - output_p95
                type: object
              sku:
                default: regional
                enum:
                - regional
                - multiRegion
                - hardwarePinned
                type: string
              tenant:
                type: string
              termEnd:
                description: RFC 3339 date the term ends.
                type: string
              termMonths:
                description: 1, 3, or 6 months.
                enum:
                - 1
                - 3
                - 6
                type: integer
              tier:
                enum:
                - interactive
                - agentic
                - standard
                type: string
            required:
            - cus
            - model
            - shape
            - tenant
            - termEnd
            - termMonths
            - tier
            type: object
        required:
        - spec
        title: CapacityReservation
        type: object
    served: true
    storage: true
